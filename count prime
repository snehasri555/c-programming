#include<stdio.h>
main()
{
	int i,j,n2,count;
	scanf("%d",&n2);
	for(i=1;i<=n2;i++)
	{
		count=0;
		for(j=2;j<=i/2;j++)
	{
    if(i%j==0)
	{
		count++;
		break;
	}
    }
    if(count==0&&i!=1)
    {
    	printf("%d ",i);
	}
}
}
