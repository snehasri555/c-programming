#include<stdio.h>
main()
{
	int n,r,m,sum=0;
	scanf("%d",&n);
	m=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(sum==m)
	printf("%d is an armstrong number",m);
	else
	printf("%d is not an armstrong number",m);
}
