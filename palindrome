#include<stdio.h>
main()
{
	int n,m,r,rev=0;
	scanf("%d",&n);
	m=n;
	while(n>0)
	{
	  r=n%10;
	  rev=(rev*10)+r;	
	  n=n/10;	
	}
	if(m==rev)
	printf("%d is palindrome",m);
	else
	printf("%d is not a palindrome",m);
}
